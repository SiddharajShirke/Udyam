# Test-only dependencies — not needed to run the service, only to run
# apps/ai-engine/tests/. Install with: pip install -r requirements-dev.txt
-r requirements.txt
pytest==8.3.3
pytest-asyncio==0.24.0
respx==0.21.1
